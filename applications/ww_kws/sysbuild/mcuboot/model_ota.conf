# Model images are optional: a missing or invalid model must not block boot.
CONFIG_BOOT_OPTIONAL_IMAGES=y
# Image 1 = wakeword model, image 2 = keyword-spotting model.
CONFIG_BOOT_OPTIONAL_IMAGES_MASK=0x6
